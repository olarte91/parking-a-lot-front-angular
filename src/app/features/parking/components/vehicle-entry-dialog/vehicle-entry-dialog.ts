import { ChangeDetectionStrategy, Component } from '@angular/core';

@Component({
  selector: 'app-vehicle-entry-dialog',
  imports: [],
  templateUrl: './vehicle-entry-dialog.html',
  changeDetection: ChangeDetectionStrategy.OnPush,
})
export class VehicleEntryDialog { }
